{
  "name": "@db-ui/foundations",
  "version": "0.0.0",
  "description": "Provides basic tokens and assets based on DB UX Design System Core.",
  "repository": {
    "type": "git",
    "url": "https://github.com/db-ui/mono.git"
  },
  "license": "Apache-2.0",
  "bin": {
    "generate-icon-fonts": "build/scripts/generate-icon-fonts/index.js"
  },
  "main": "build.js",
  "files": [
    "assets",
    "build"
  ],
  "scripts": {
    "build": "npm-run-all build:*",
    "build:01_normalize": "npm run copy-normalize",
    "build:02_copy": "npm run copy:scss",
    "build:03_css": "sass --no-source-map --load-path=node_modules/ --load-path=../../node_modules/ build/scss/:build/css/ --future-deprecation=import",
    "build:04_tailwind": "cpy tailwind build",
    "build:05_postcss": "postcss build/css/**/*.css --replace",
    "clean": "rm -rf build",
    "copy-build": "npm-run-all copy-build:*",
    "copy-build:assets": "cpy assets ../../build-outputs/foundations",
    "copy-build:package.json": "cpy package.json ../../build-outputs/foundations",
    "copy-build:readme": "cpy README.md ../../build-outputs/foundations",
    "copy-build:web": "cpy build ../../build-outputs/foundations",
    "copy-normalize": "cpy ../../node_modules/@csstools/normalize.css/normalize.css scss --flat --rename=_normalize.scss",
    "copy:scss": "cpy scss build",
    "dev": "vite --open",
    "generate:icon-fonts": "npx @db-ui/gif --src ./assets/icons/functional --variants filled inverted --fontName db-ux --withSizes true --debug true",
    "prepare": "npm run copy-normalize",
    "regenerate:screenshots": "npx playwright test -c ./test/playwright.config.mjs --update-snapshots",
    "start": "nodemon --config nodemon.json",
    "test:e2e": "npx playwright test --config=./test/playwright.config.mjs"
  },
  "devDependencies": {
    "@csstools/normalize.css": "12.1.1",
    "cpy-cli": "^5.0.0",
    "cssnano": "^7.0.3",
    "dotenv": "^16.4.5",
    "nodemon": "3.1.3",
    "sass": "^1.77.6"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  }
}
