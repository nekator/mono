{
  "name": "@db-ui/core",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "generate:component": "hygen mitosis new",
    "prebuild": "sass src --no-source-map --load-path=node_modules/ --load-path=../../node_modules/",
    "build": "mitosis build",
    "postbuild": "node scripts/post-build/index.js && cpr output/next/src/components ../../showcases/next-showcase/components -o",
    "prestart": "npm run build",
    "start": "watch \"npm run build\" ./src ./overrides ./scripts/post-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@builder.io/mitosis": "^0.0.80",
    "@builder.io/mitosis-cli": "^0.0.38"
  },
  "devDependencies": {
    "@angular/core": "~13.1.0",
    "@builder.io/eslint-plugin-mitosis": "^0.0.10",
    "cpr": "3.0.1",
    "eslint": "^8.17.0",
    "hygen": "^6.2.11",
    "lit": "^2.3.1",
    "sass": "^1.55.0",
    "watch": "^1.0.2"
  }
}
